import 'package:flutter/widgets.dart';

class NomoSider extends StatelessWidget {
  const NomoSider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
