import 'package:flutter/material.dart';

class NomoModalSheet extends StatelessWidget {
  const NomoModalSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
