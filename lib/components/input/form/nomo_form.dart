import 'package:flutter/widgets.dart';

class NomoForm extends StatelessWidget {
  const NomoForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
