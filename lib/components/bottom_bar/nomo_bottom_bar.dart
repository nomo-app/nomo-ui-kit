import 'package:flutter/widgets.dart';

class NomoBottomBar extends StatelessWidget {
  const NomoBottomBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
